const sample1='madame'
const sample2='adame'
class Sequence{
    constructor(firstString,secondString){
        this.firstString=firstString;
        this.secondString=secondString;

    }
    findSubSequence(){
//lc, duh
//checks 1st letter in a string and checks if its in the second one, if so write it down
//then add letters and do the same, if it can be found there too then
//maybe regex?
    }
}


if(sample1.includes(sample2[5])){
    console.log('zawiera');
}
else{
    console.log('nie zawiera')
}